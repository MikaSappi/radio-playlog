// Copy to config.js and edit per-environment.
//
// Deployed via Firebase Hosting with rewrites to the ui-api Cloud Run
// service — leave API_BASE empty so everything is same-origin:
//   window.API_BASE = "";
//
// Local dev hitting a remote ui-api directly — point at the CR URL:
//   window.API_BASE = "https://radio-playlog-ui-api-xxxx.a.run.app";
window.API_BASE = "";
